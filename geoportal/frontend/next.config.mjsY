/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  experimental: {
    outputFileTracingRoot: undefined,
  },
  env: {
    NEXT_PUBLIC_API_URL: 'https://aplicativosgrd.crantioquia.org.co/api'
  },
  async rewrites() {
    return [
      {
        source: '/api/:path*',
        destination: 'http://45.55.212.201:8000/api/:path*'
      }
    ];
  },
  // Remover assetPrefix que puede causar problemas
  basePath: '',
  trailingSlash: false
};

export default nextConfig;
